{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
    <section>
        {%for post in posts%}
            <div class="post">
                <a href="">
                    <h2>{{ post.title}}</h2>
                    <a href=""></a>
                </a>
            </div>
        {%endfor%}
    </section>
{% endblock %}

