{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
    <section>
        {%for post in posts%}
            <div class="post">
                <a href="">
                    <h2>{{ post.title}}</h2>
                    <img src="public/uploads/Video/{{ post.file }}" alt="">
                </a>
            </div>
        {%endfor%}
    </section>
{% endblock %}

